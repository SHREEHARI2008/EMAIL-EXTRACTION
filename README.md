# EMAIL-EXTRACTION
This project is a Python automation script that extracts email addresses from a text file and stores them in another file. It validates emails using string operations without relying on regular expressions. The project demonstrates file handling, functions, and data processing for real-world repetitive tasks.
